# Copy to .env.local and fill in. Never commit .env or .env.local (see .gitignore).
# On Render, set these in the dashboard Environment tab; keep tokens/keys secret.

# Square (use sandbox for dev; fake card for testing: 4111 1111 1111 1111, any future expiry, any CVC)
NEXT_PUBLIC_SQUARE_APPLICATION_ID=
NEXT_PUBLIC_SQUARE_LOCATION_ID=
SQUARE_ACCESS_TOKEN=
SQUARE_ENVIRONMENT=sandbox
NEXT_PUBLIC_SQUARE_ENVIRONMENT=sandbox
SQUARE_WEBHOOK_SIGNATURE_KEY=

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Email (Resend – or use Gmail SMTP below for free, no domain verification)
RESEND_API_KEY=
EMAIL_FROM=
EMAIL_REPLY_TO=
ORDER_NOTIFY_EMAILS=support@millieshomemade.com

# Optional: Gmail SMTP (free, no DNS). Sends to any address. Requires 2FA + App Password.
# USE_SMTP_GMAIL=1
# SMTP_USER=your@gmail.com
# SMTP_APP_PASSWORD=xxxx xxxx xxxx xxxx

# App (use your Render URL in production, e.g. https://your-app.onrender.com)
NEXT_PUBLIC_APP_URL=http://localhost:3000
